import {
  ALIVE_BORN_COLOR,
  ALIVE_COLOR,
  ALIVE_STABLE_COLOR,
  BACKGROUND_COLOR,
  BORDER_RADIUS,
  BORDER_WIDTH,
  DEAD_COLOR,
  ITERATION_DELAY,
} from "./constants";

const theme = {
  colors: {
    alive: ALIVE_COLOR,
    alive_born: ALIVE_BORN_COLOR,
    alive_stable: ALIVE_STABLE_COLOR,
    dead: DEAD_COLOR,
    background: BACKGROUND_COLOR,
  },
  iteration_delay: ITERATION_DELAY,
  border_radius: BORDER_RADIUS,
  border_width: BORDER_WIDTH,
};

export default theme;
